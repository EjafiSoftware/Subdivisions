namespace Subdivisions.Core
{
    /// <summary>Which kind of snap candidate wins when two are coincident (within the coincidence radius).</summary>
    public enum SnapPreference
    {
        Net,
        Area
    }
}
